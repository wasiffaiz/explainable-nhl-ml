# Models Folder

Saved model files are not included in this repository by default.

Files such as `.pkl`, `.joblib`, `.h5`, and `.keras` are ignored through `.gitignore` to keep the repository lightweight and clean.

To recreate the model, run the notebooks in order.
